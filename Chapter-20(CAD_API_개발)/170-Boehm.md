
# Boehm 공식 

- Boehm 공식은 B‑spline의 핵심 중 핵심인데, 대부분의 책에서도 설명이  
    너무 짧아서 “왜 이렇게 되는지”가 잘 안 보임.
- 여기서는 직관 → 수식 → 알고리즘 → 예시 순서로 아주 깊게 풀어서 설명.

## 🎯 1. Boehm 공식이 뭔데?
- Boehm 공식은 B‑spline knot insertion을 위한 공식.
- 즉,
    - B‑spline 함수의 형상을 바꾸지 않고, knot vector에 새로운 knot을  
        삽입할 때 control value를 어떻게 갱신해야 하는가를 해결하는 공식

## 🎯 2. 왜 knot insertion이 필요한가?
- B‑spline은 다음 형태로 정의:
```math
F(u)=\sum _{i=0}^nf_i\, N_{i,p}(u)
```
- 여기서
    - $f_i$: control value
    - $N_{i,p}(u)$: B‑spline basis
    - p: degree
    - knot vector: $U=\{ U_0,\dots ,U_m\}$ 
- 이제 knot x를 삽입하면:
    - basis 함수가 바뀌고
    - control value 개수가 하나 늘어나며
    - 새로운 basis $N'_{i,p}(u)$ 가 생김
- 그런데 형상은 절대 바뀌면 안 됨.
- 즉,
```math
F(u)=\sum _{i=0}^nf_iN_{i,p}(u)=\sum _{i=0}^{n+1}f'_iN'_{i,p}(u)
```
- 이걸 만족하는 $f'_i$ 를 구하는 것이 Boehm 공식의 목적.

## 🎯 3. Boehm 공식의 핵심 아이디어
- ✔ B‑spline basis는 **local support** 를 가진다
```math
N_{i,p}(u)\neq 0\quad \mathrm{only\  when\  }U_i\leq u<U_{i+p+1}
```
- 즉, knot x를 삽입하면 영향을 받는 basis는 딱 p개뿐이다.
- 그래서 control value도 일부만 바뀐다.

## 🎯 4. Boehm 공식의 수식
- 삽입 knot x가 다음 구간에 있다고 하자:
```math
U_k\leq x<U_{k+1}
```
- 그러면 새로운 control 값은:
```math
f'_i=\left\{ \, \begin{array}{ll}\textstyle f_i,&\textstyle i\leq k-p\\ \textstyle \alpha _if_i+(1-\alpha _i)f_{i-1},&\textstyle k-p<i\leq k\\ \textstyle f_{i-1},&\textstyle i>k\end{array}\right.
```
- 여기서
```math
\alpha_i =
\begin{cases}
\dfrac{U_{i+p} - x}{\,U_{i+p} - U_i\,}, & U_i < U_{i+p} \\
0, & \text{degenerate case}
\end{cases}
```

## 🎯 5. α(알파)의 의미
```math
\alpha _i=\frac{U_{i+p}-x}{U_{i+p}-U_i}
```
- 이 값은 기존 control value 두 개를 섞는 비율.
- x가 $U_i$ 에 가까우면 → $\alpha _i$ 가 1에 가까움
    - $f'_i\approx f_i$
- x가 $U_{i+p}$ 에 가까우면 → $\alpha _i$ 가 0에 가까움
    - $f'_i\approx f_{i-1}$
- 즉, knot 삽입 위치에 따라 control value가 자연스럽게 **블렌딩** 되는 구조.

## 🎯 6. 왜 이 공식이 형상을 보존할까?
- 핵심은 다음 항등식이다:
```math
N_{i,p}(u)=\alpha _iN'_{i,p}(u)+(1-\alpha _{i+1})N'_{i+1,p}(u)
```
- 즉, 기존 basis는 새로운 basis의 선형 결합으로 표현된다.
- 따라서:
```math
F(u)=\sum _if_iN_{i,p}(u)
```
- 에 위 항등식을 대입하면:
```math
F(u)=\sum _if'_iN'_{i,p}(u)
```
- 이 되도록 $f'_i$ 를 선택할 수 있고, 그 선택이 바로 Boehm 공식이다.

## 🎯 7. 직관적 예시 (degree 1)
- degree 1 (linear) B‑spline은 사실상 선형 보간이다.
- 예를 들어:
- control 값: $f_0$, $f_1$
- knot: [0,0,1,1]
- 여기에 x=0.3을 삽입하면:
```math
\alpha =\frac{1-0.3}{1-0}=0.7
```
새 control 값:
- $f'_0=f_0$
- $f'_1=0.7f_1+0.3f_0$
- $f'_2=f_1$
- 즉, 선분을 0.3 지점에서 분할한 것과 동일하다.

## 🎯 8. Boehm 공식의 중요한 성질
- ✔ 1) 형상 보존
```math
F(u)=F'(u)
```
- ✔ 2) local support 유지
    - 영향받는 control 값은 최대 p개.
- ✔ 3) 반복 적용 가능
    - 여러 knot을 삽입하면 단일 삽입을 여러 번 적용한 것과 동일.
- ✔ 4) 역순 삽입도 가능
- 그래서 C 코드와 코드처럼 뒤에서부터 삽입해도 수학적으로 동일.

## 🎯 9. 코드와 Boehm 공식의 정확한 매핑
- 핵심 코드:
```rust
alf = (U[k+l] - x) / (U[k+l] - U[i-p+l]);
fq[t-1] = (1-alf) * fq[t] + alf * fq[t-1];
```

- 이건 정확히:
```math
f'_{i-1}=(1-\alpha _i)f'_i+\alpha _if_{i-1}
```
- 과 1:1 대응한다.

## 🎯 10. 결론
- Boehm 공식은:
    - B‑spline knot insertion의 유일한 정답
    - 형상 보존을 보장하는 수학적 기반
    - basis 변환 항등식에서 유도됨
    - control value를 자연스럽게 블렌딩하는 구조

---
