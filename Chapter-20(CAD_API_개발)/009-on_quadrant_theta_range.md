# on_quadrant_theta_range

이 함수는 단위원(circle) 또는 방향 벡터에서 각도를 기준으로 **사분면(quadrant)** 을 구분하는 데 사용됩니다.  
아래에 수학적 의미와 각 단계의 해석을 정리.

## 소스 코드
```rust
/// Quadrant (East, North, West, South = 0,1,2,3) angle range
pub fn on_quadrant_theta_range(q: i32) -> (f64, f64) {
    let p8 = PI * 0.25;
    match q {
        0 => (-p8, p8),            // East
        1 => (p8, 3.0 * p8),       // North
        2 => (3.0 * p8, 5.0 * p8), // West
        _ => (5.0 * p8, 7.0 * p8), // South
    }
}
```

## 🧮 수학적 의미
### ✅ 기본 정의
- PI는 $\pi$ 를 의미하며, $p8 = \frac{\pi}{4}$ 는 45도입니다
- 단위원에서 각도 $\theta$ 는 일반적으로 x축 기준 반시계 방향으로 측정됩니다
- 사분면은 다음과 같이 정의됩니다:

## 📐 사분면 각도 범위 요약

| 방향   | 사분면 번호 | 각도 범위 (라디안)                         |
|--------|--------------|--------------------------------------------|
| East   | 0            | $\left[-\frac{\pi}{4}, \frac{\pi}{4} \right]$ |
| North  | 1            | $\left[\frac{\pi}{4}, \frac{3\pi}{4} \right]$ |
| West   | 2            | $\left[\frac{3\pi}{4}, \frac{5\pi}{4} \right]$ |
| South  | 3            | $\left[\frac{5\pi}{4}, \frac{7\pi}{4} \right]$ |



## 📐 왜 이런 범위인가?
- 단위원에서 각도는 일반적으로 $-\pi$ 부터 $\pi$  또는 $0$ 부터 $2\pi$ 까지 표현됩니다
- 이 함수는 중심이 원점인 단위원에서 방향 벡터의 각도를 기준으로 사분면을 나누는 방식입니다
- 예를 들어, 벡터가 $\theta =0$ 이면 동쪽(East), $\theta =\frac{\pi }{2}$ 이면 북쪽(North)

## 🔍 코드 해석
```rust
let p8 = ON_PI * 0.25; // π/4 = 45도
```

- 기준 각도 단위: 45도
```rust
match q {
    0 => (-p8, p8),            // East
    1 => (p8, 3.0 * p8),       // North
    2 => (3.0 * p8, 5.0 * p8), // West
    _ => (5.0 * p8, 7.0 * p8), // South
}
```

- 각 사분면은 90도(= $\frac{\pi }{2}$ ) 범위로 나뉨
- 각도 기준으로 방향을 구분할 수 있도록 설계됨

## 📌 요약

| 방향   | 사분면 번호 | 각도 범위 (라디안)                         | 각도 범위 (도)     |
|--------|--------------|--------------------------------------------|--------------------|
| East   | 0            | $\left[-\frac{\pi}{4}, \frac{\pi}{4} \right]$ | [−45°, 45°]        |
| North  | 1            | $\left[\frac{\pi}{4}, \frac{3\pi}{4} \right]$ | [45°, 135°]        |
| West   | 2            | $\left[\frac{3\pi}{4}, \frac{5\pi}{4} \right]$ | [135°, 225°]       |
| South  | 3            | $\left[\frac{5\pi}{4}, \frac{7\pi}{4} \right]$ | [225°, 315°]       |

- 이 함수는 벡터의 방향을 기반으로 사분면을 판별하거나, 특정 방향에 속하는 각도 범위를 계산할 때 유용합니다.



## ✅ 방향 판별 함수: on_direction_quadrant(y, x)
```rust
/// Returns the quadrant index (0: East, 1: North, 2: West, 3: South)
pub fn on_direction_quadrant(y: f64, x: f64) -> i32 {
    let theta = y.atan2(x); // angle in radians
    let p8 = std::f64::consts::PI * 0.25;

    if theta >= -p8 && theta < p8 {
        0 // East
    } else if theta >= p8 && theta < 3.0 * p8 {
        1 // North
    } else if theta >= 3.0 * p8 && theta < 5.0 * p8 {
        2 // West
    } else {
        3 // South
    }
}
```


## 📐 수학적 의미
- atan2(y, x)는 벡터의 방향을 라디안으로 반환하며, 범위는 $[-\pi ,\pi ]$

## 📌 각도 구간에 따른 사분면 분류

| 방향   | 사분면 번호 | 각도 범위 (라디안)                         |
|--------|--------------|--------------------------------------------|
| East   | 0            | $\left[-\frac{\pi}{4}, \frac{\pi}{4} \right)$ |
| North  | 1            | $\left[\frac{\pi}{4}, \frac{3\pi}{4} \right)$ |
| West   | 2            | $\left[\frac{3\pi}{4}, \frac{5\pi}{4} \right)$ |
| South  | 3            | $\left[\frac{5\pi}{4}, \frac{7\pi}{4} \right)$ |



## 🧪 예시
```rust
direction_quadrant(0.0, 1.0); // → 0 (East)
direction_quadrant(1.0, 0.0); // → 1 (North)
direction_quadrant(0.0, -1.0); // → 2 (West)
direction_quadrant(-1.0, 0.0); // → 3 (South)
```
---

