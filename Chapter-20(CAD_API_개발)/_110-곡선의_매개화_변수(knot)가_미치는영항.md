# Knots Reparameter
- NURBS에서 knot vector의 값 자체는 **절대적인 숫자** 라기보다는 **비율(파라미터 공간)** 을 정의하는 역할을 합니다.
- 그래서 보통 [0,1] 구간을 쓰지만, 동일한 비율로 [2,3] 같은 다른 구간을 쓰면 곡선/서피스의 형태 자체는 변하지 않습니다.

  
## 🔑 이유
- Basis function은 knot 구간의 상대적 위치에 따라 정의됩니다.
  - 예를 들어, [0,1]에서 는 중간 지점이고, [2,3]에서  역시 중간 지점입니다.
    - 두 경우 모두 **중간** 이라는 의미는 동일합니다.
- 즉, knot vector를 선형적으로 스케일링(affine transform)하면 파라메트릭 도메인만 바뀌고, 기하학적 곡선/서피스는 동일합니다.
- 단, 파라메트릭 속도(예: 곡선 위에서 파라미터 u에 따른 변화율)는 달라질 수 있습니다.
- [0,1]에서 u=0.5와 [2,3]에서 u=2.5는 같은 위치를 가리키지만, 도함수 계산 시  
  분모에 들어가는 knot 간격이 달라져서 파라미터에 따른 미분 값은 스케일링됩니다

## 📌 정리
- Knot 구간을 $[0,1] → [2,3]$ 으로 바꾸면 곡선/서피스의 모양은 그대로입니다.
- 차이점은:
  - 파라메트릭 도메인 범위가 달라짐 $(u ∈ [0,1] → u ∈ [2,3])$
  - 파라메트릭 속도/미분 값이 스케일링됨

- 👉 쉽게 말해, **좌표축 눈금을 바꾼 것** 과 같음.
- 눈금이 03이든, 그래프 모양은 같지만 x축 값의 범위가 달라지는 것과 동일합니다.

---

## 곡선 재매개변수화와 도함수 스케일링
- 곡선 C(u)가 있다고 할 때, 매개변수를 선형 변환으로 바꾸면
  
$$
u'=a\cdot u+b\quad (a>0)
$$

- 새로운 곡선은

$$
\tilde {C}(u')=C \left( \frac{u'-b}{a}\right) 
$$

이 됩니다.

- 이때 도함수(접선 벡터)는 연쇄법칙에 의해

$$
\frac{d\tilde {C}}{du'}(u')=\frac{1}{a} \frac{dC}{du} \left( \frac{u'-b}{a}\right) 
$$

가 됩니다.

- 의미
  - 스케일링만 바꿀 때 $([0,1]\rightarrow [0,2], 즉 a=2)$:
    - 접선 벡터의 크기가 1/2로 줄어듭니다.
    - 곡선 모양은 같지만, 파라미터에 따른 속도가 달라집니다.
  - 단순 이동만 할 때 ($[0,1]\rightarrow [2,3]$, 즉 $a=1,b=2$):
    - 접선 벡터는 그대로이고, 단지 파라미터 값이 옮겨진 것뿐입니다.

- 호길이(arc length)와 속도
  - 호길이는 변하지 않습니다.
    
$$
ds=\left\| \frac{dC}{du}\right\|  du=\left\| \frac{d\tilde {C}}{du'}\right\|  du'
$$

- 즉, 파라미터가 바뀌어도 실제 곡선의 길이는 동일합니다.
- 다만 **파라미터에 따른 속도** 가 $1/a$ 배로 조정됩니다.

- 서피스의 경우
  - 서피스 S(u,v)에서  

$$
u'=au+b,\quad v'=cv+d
$$

로 바꾸면

$$
\frac{\partial \tilde {S}}{\partial u'}=\frac{1}{a}\frac{\partial S}{\partial u},\quad \frac{\partial \tilde {S}}{\partial v'}=\frac{1}{c}\frac{\partial S}{\partial v}
$$

가 됩니다.
- 따라서 법선 벡터는

$$
\frac{\partial \tilde {S}}{\partial u'}\times \frac{\partial \tilde {S}}{\partial v'}=\frac{1}{ac}\left( \frac{\partial S}{\partial u}\times \frac{\partial S}{\partial v}\right) 
$$

로 스케일링되지만, 방향은 동일합니다.

## ✅ 정리:
- Knot 구간을 단순히 이동하면 곡선/서피스의 도함수는 그대로.
- Knot 구간을 스케일링하면 도함수는 1/a 배로 크기가 변함.
- 곡선/서피스의 형태와 길이는 변하지 않고, 파라미터 속도만 달라집니다.

## 1. 원의 파라메트릭 표현
- 단위 원을 보통 이렇게 표현합니다:

$$
C(u)=(\cos u,\sin u)
$$

- 만약 $u\in [0,2\pi ]$ 라면, 한 바퀴를 돌 때 파라미터가 $0\rightarrow 2\pi$ 까지 변합니다.
- 만약 $u\in [0,1]$ 로 설정하고 싶다면, 선형 매핑을 통해

$$
u'=2\pi u
$$

로 바꿔줍니다.

## 2. 도함수 비교
- 원래 도함수:

$$
\frac{dC}{du}(u)=(-\sin u,\cos u)
$$

  - 크기는 항상 1 (즉, 단위 속도).
- 재매개변수화한 곡선:

$$
\tilde {C}(u')=C(2\pi u')=(\cos (2\pi u'),\sin (2\pi u'))
$$

- 도함수는

$$
\frac{d\tilde {C}}{du'}=2\pi \cdot (-\sin (2\pi u'),\cos (2\pi u'))
$$

- 즉, 크기는 $2\pi$ .

## 3. 스케일링 관계
- 일반적으로

$$
\frac{d\tilde {C}}{du'}=a\cdot \frac{dC}{du}
$$

인데 여기서 $a=2\pi$ 입니다.
- 따라서 도함수(접선 벡터)의 크기가 $2\pi$ 배 커집니다.
- 반대로, 만약 $[0,2\pi ]$ 에서 $[0,1]$ 로 줄였다면, 도함수는 $1/(2\pi )$ 배로 작아집니다.

## 4. 정리
- Knot 구간을 $[0,2\pi ] → [0,1]$ 로 바꾸면 곡선 모양은 동일합니다.
- 하지만 도함수(접선 벡터)는 $1/(2\pi )$ 배로 스케일링됩니다.
- 즉, 형태는 유지되지만 파라미터 속도(미분 값)는 달라진다는 말씀이 정확합니다.

- 👉 원 예시로 보면, $[0,2\pi ]$ 에서 접선 벡터 크기는 1인데, [0,1]로 바꾸면 접선 벡터 크기는 $2\pi$ 가 됩니다.  
  반대로 $[0,1]$ 에서 $[0,2\pi ]$ 로 늘리면 접선 벡터 크기는 $1/(2\pi )$ 로 줄어듭니다

---

## 2차 미분

재매개변수화를 하면 **1차 미분(접선 벡터)** 뿐만 아니라 **2차 미분(곡률 관련 값)** 도 스케일링됩니다.

- 일반적인 경우
- 곡선 C(u)를

$$
u'=au+b
$$

로 바꾸면, 새로운 곡선은

$$
\tilde {C}(u')=C\! \left( \frac{u'-b}{a}\right) 
$$

이고,

- 1차 미분:

$$
\frac{d\tilde {C}}{du'}=\frac{1}{a}\frac{dC}{du}
$$

- 2차 미분:

$$
\frac{d^2\tilde {C}}{du'^2}=\frac{1}{a^2}\frac{d^2C}{du^2}
$$

- 즉, 2차 미분은 $1/a^2$ 배로 더 작아지거나 커집니다.

- 원(circle) 예시
  - 단위 원:

$$
C(u)=(\cos u,\sin u),\quad u\in [0,2\pi ]
$$

- 원래 도함수:

$$
C'(u)=(-\sin u,\cos u),\quad \| C'(u)\| =1
$$

- 원래 2차 미분:

$$
C''(u)=(-\cos u,-\sin u),\quad \| C''(u)\| =1
$$

- 파라미터를 [0,1]로 바꿀 때

$$  
u'=\frac{u}{2\pi }
$$

- 1차 미분:

$$
\frac{d\tilde {C}}{du'}=2\pi \cdot (-\sin (2\pi u'),\cos (2\pi u')),\quad \| C'(u')\| =2\pi 
$$

- 2차 미분:

$$
\frac{d^2\tilde {C}}{du'^2}=(2\pi )^2\cdot (-\cos (2\pi u'),-\sin (2\pi u')),\quad \| C''(u')\| =(2\pi )^2
$$

## 정리
- **1차 미분(접선 벡터)** 는 $1/a$ 배로 스케일링.
- **2차 미분(곡률 관련 벡터)** 는 $1/a^2$ 배로 스케일링.
- 따라서 파라미터 구간을 줄이면(예: $[0,2\pi ]\rightarrow [0,1]$ ) 2차 미분은 더 커집니다.

---

## curvature

- **곡률(curvature)** 은 파라미터를 어떻게 설정하든 기하학적 형태에만 의존하기 때문에,  
- 단순히 knot 구간을 바꾸거나 파라미터를 선형 스케일링해도 곡률 값은 변하지 않습니다.

## 1. 곡률 정의
- 곡선 C(u)에서 곡률 \kappa (u)는 다음과 같이 정의됩니다:

$$
\kappa (u)=\frac{\| C'(u)\times C''(u)\| }{\| C'(u)\| ^3}
$$

- C'(u): 접선 벡터
- C''(u): 2차 도함수(곡선의 굽힘 정도)
- 분자: 곡선이 얼마나 꺾이는지
- 분모: 접선 벡터의 크기에 대한 정규화

## 2. 파라미터 스케일링 효과
- 만약 파라미터를

$$
u'=au+b
$$

로 바꾸면,
- 1차 미분: 
- 2차 미분: 
- 이를 곡률 공식에 대입하면:

$$
\kappa (u)=\frac{\| C'(u)\times C''(u)\| }{\| C'(u)\| ^3}
$$

- 즉, 스케일링 인자 a가 완전히 소거되어 곡률은 변하지 않습니다.

## 3. 원(circle) 예시
  - 단위 원 $C(u)=(\cos u,\sin u)$:
  - 원래 곡률: $\kappa =1$ (항상 일정)
- 파라미터를 $[0,2\pi ] → [0,1]$ 로 바꿔도,
  - 접선 벡터와 2차 미분은 각각 $2\pi$ , $(2\pi )^2$ 배로 커지지만,
  - 곡률 계산에서는 이 배율이 상쇄되어 여전히 $\kappa =1$.

## ✅ 정리
- 파라미터 구간을 바꾸면 도함수 크기는 변하지만,
- 곡률은 기하학적 성질이므로 변하지 않습니다.
- 따라서 knot 구간을 [0,1]이든 [0,2\pi ]든 동일한 곡률을 갖습니다.

---


## 1. 곡률 공식
- 곡선 C(u)의 곡률은

$$
\kappa (u)=\frac{\| C'(u)\times C''(u)\| }{\| C'(u)\| ^3}
$$


## 2. 파라미터 스케일링
- 새로운 파라미터를

$$
u'=au+b
$$

로 정의하면, 재매개변수화된 곡선은

$$
\tilde {C}(u')=C\! \left( \frac{u'-b}{a}\right) 
$$

## 3. 도함수 변환
- 1차 도함수:

$$
\tilde {C}'(u')=\frac{1}{a}C'(u)
$$

- 2차 도함수:

$$
\tilde {C}''(u')=\frac{1}{a^2}C''(u)
$$

## 4. 곡률 공식에 대입

$$
\kappa (u')=\frac{\| \tilde {C}'(u')\times \tilde {C}''(u')\| }{\| \tilde {C}'(u')\| ^3}
$$

- 대입하면:

## 5. 스케일링 인자 소거
- 분자와 분모의 가 완전히 없어져서:

$$  
\kappa (u')=\frac{\| C'(u)\times C''(u)\| }{\| C'(u)\| ^3}=\kappa (u)
$$

---





