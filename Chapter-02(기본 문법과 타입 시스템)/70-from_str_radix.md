# from_str_radix
`from_str_radix` 는 Rust에서 문자열을 **특정 진법(radix)** 으로 해석하여 숫자로 변환할 때 사용하는 함수입니다.  
아래에 자세히 설명.

## 🔢 from_str_radix란?
`from_str_radix` 는 문자열을 2진수, 8진수, 10진수, 16진수 등 원하는 진법으로 해석해서 숫자로 변환하는 함수입니다.  

```rust
use std::num::ParseIntError;

fn main() -> Result<(), ParseIntError> {
    let binary = i32::from_str_radix("1010", 2)?;  // 2진수 → 10
    let octal = i32::from_str_radix("12", 8)?;     // 8진수 → 10
    let decimal = i32::from_str_radix("10", 10)?;  // 10진수 → 10
    let hex = i32::from_str_radix("A", 16)?;       // 16진수 → 10

    println!("{}, {}, {}, {}", binary, octal, decimal, hex);
    Ok(())
}
```

## 📌 함수 시그니처
```rust
impl<T> FromStrRadix for T
where
    T: Num,
{
    fn from_str_radix(src: &str, radix: u32) -> Result<T, ParseIntError>
}
``
- src: 변환할 문자열
- radix: 진법 (2~36 사이의 값)
- 반환값: Result<T, ParseIntError> → 실패 시 오류 처리 가능

## 🧠 왜 필요한가?
- 일반적인 str.parse()는 10진수만 처리합니다.
- from_str_radix는 진법을 직접 지정할 수 있어 유연한 숫자 파싱이 가능합니다.  
예:
```rust
let n = u32::from_str_radix("FF", 16)?; // 255
```

## ⚠️ 주의사항
- 진법은 `2 <= radix <= 36` 사이여야 합니다.
- 문자열은 해당 진법에 맞는 문자만 포함해야 합니다.
- 예: "G"는 16진수로는 오류 발생
- 반환값은 Result이므로 `unwrap()` 또는 `?` 로 처리해야 합니다.

## ✅ from_str_radix 요약

| 항목         | 설명                                               |
|--------------|----------------------------------------------------|
| 목적         | 문자열을 특정 진법(radix)으로 숫자로 변환          |
| 진법 범위    | 2 ~ 36                                             |
| 반환 타입    | Result<T, ParseIntError>                           |
| 사용 예      | i32::from_str_radix("FF", 16) → Ok(255)            |


---

