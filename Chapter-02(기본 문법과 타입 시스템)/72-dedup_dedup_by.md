# dedup / dedup_by
Rust에서 Vec::dedup()은 연속된 중복 요소만 제거합니다.  
전체 중복 제거를 위해서는 정렬하거나 HashSet을 사용하는 방법이 필요합니다.  
Rust에서 벡터의 중복 요소를 제거하는 방법은 여러 가지가 있으며, 목적에 따라 적절한 방법을 선택해야 합니다:

## 🧹 1. Vec::dedup() 메서드
- 기능: 벡터에서 연속된 중복 요소만 제거합니다.
- 사용 예시:
```rust
let mut v = vec![1, 2, 2, 3, 3, 3, 4];
v.dedup();
println!("{:?}", v); // 출력: [1, 2, 3, 4]
```
- 주의: 중복이 연속되지 않으면 제거되지 않습니다.
```rust
let mut v = vec![1, 2, 1, 2];
v.dedup();
println!("{:?}", v); // 출력: [1, 2, 1, 2]
```


## 🔀 2. 정렬 후 dedup() 사용
- 방법: 먼저 sort()로 정렬한 후 dedup()을 호출하면 전체 중복 제거 가능.
- 예시:
```rust
let mut v = vec![1, 2, 1, 2, 3];
v.sort();
v.dedup();
println!("{:?}", v); // 출력: [1, 2, 3]
```
- 단점: 원래 순서가 유지되지 않음.

## 🧺 3. HashSet을 이용한 중복 제거
- 방법: HashSet은 중복을 허용하지 않기 때문에, 벡터를 HashSet으로 변환 후 다시 Vec으로 변환.
- 예시:
```rust
use std::collections::HashSet;

let v = vec![1, 2, 1, 3];

let v: Vec<_> = v.into_iter().collect::<HashSet<_>>().into_iter().collect();

println!("{:?}", v); // 출력: [1, 2, 3] (순서는 보장되지 않음)
```
- 장점: 빠르고 간단.
- 단점: 요소의 순서가 변경됨.

## 🧩 4. 순서 유지하며 중복 제거 (retain + HashSet)
- 방법: HashSet을 사용하면서 retain()으로 순서를 유지.
- 예시:
```rust
use std::collections::HashSet;

let mut v = vec![1, 2, 1, 3];
let mut seen = HashSet::new();
v.retain(|x| seen.insert(*x));
println!("{:?}", v); // 출력: [1, 2, 3]
```
- 장점: 원래 순서 유지 가능.

## 🧪 기타 방법: itertools::unique()
- itertools 크레이트를 사용하면 .unique()로 중복 제거 가능.
- 예시:
```rust
use itertools::Itertools;

let v = vec![1, 2, 1, 3];
let v: Vec<_> = v.into_iter().unique().collect();
println!("{:?}", v); // 출력: [1, 2, 3]

```
# sort + dedup

Rust에서 sort()와 dedup()을 함께 사용하는 방식은 전체 중복 요소를 제거할 때 매우 유용합니다.  
아래에서 이 조합의 동작 원리와 주의할 점을 자세히 설명.

## 🧠 기본 개념
- Vec::sort()는 벡터의 요소를 오름차순으로 정렬합니다.
- Vec::dedup()는 연속된 중복 요소만 제거합니다.
- 따라서 sort()로 먼저 정렬하면, 중복된 값들이 연속으로 배치되므로 dedup()이 효과적으로 중복을 제거할 수 있습니다.

## 🔍 예제 코드
```rust
fn main() {
    let mut v = vec![3, 1, 2, 3, 2, 1];
    v.sort();      // [1, 1, 2, 2, 3, 3]
    v.dedup();     // [1, 2, 3]
    println!("{:?}", v);
}
```
## ⚠️ 주의할 점
- 정렬은 원래 순서를 파괴합니다. 만약 순서가 중요하다면 다른 방법을 사용해야 합니다 (예: retain + HashSet).
- dedup()은 요소의 PartialEq을 사용하여 비교합니다. 즉, 사용자 정의 타입의 경우 PartialEq을 구현해야 합니다.

## 🧩 사용자 정의 타입에서 사용하기
```rust
#[derive(Debug, PartialEq, Eq, PartialOrd, Ord)]
struct Person {
    name: String,
    age: u32,
}
```
```rust
fn main() {
    let mut people = vec![
        Person { name: "Alice".into(), age: 30 },
        Person { name: "Bob".into(), age: 25 },
        Person { name: "Alice".into(), age: 30 },
    ];

    people.sort();   // 정렬 기준은 PartialOrd에 따라 결정됨
    people.dedup();  // 연속된 중복 제거
    println!("{:?}", people);
}
```

## 🧪 dedup_by와 dedup_by_key
- `dedup_by`: 커스텀 비교 로직을 직접 정의
- `dedup_by_key`: 특정 키 값 기준으로 중복 제거
```rust
let mut v = vec!["apple", "apricot", "banana", "blueberry"];
v.sort_by_key(|s| s.chars().next()); // 첫 글자 기준 정렬
v.dedup_by_key(|s| s.chars().next()); // 첫 글자 기준 중복 제거
println!("{:?}", v); // ["apple", "banana"]
```
## ✅ 요약

| 방법               | 중복 제거 범위 | 순서 유지 | 성능   | 비고                         |
|--------------------|----------------|-----------|--------|------------------------------|
| sort + dedup       | 전체 제거      | ❌        | 빠름   | 정렬로 인해 원래 순서 파괴됨 |
| retain + HashSet   | 전체 제거      | ✅        | 보통   | 순서 유지 가능               |
| dedup              | 연속된 중복만 | ✅        | 빠름   | 정렬되지 않은 중복은 제거 안됨 |

---

# retain / HashSet

Rust에서 retain과 HashSet을 함께 사용하는 방식은 중복을 제거하면서 원래 벡터의 순서를 유지하고 싶을 때 매우 유용합니다.  
아래에서 이 조합의 동작 원리, 예제, 장단점을 자세히 설명.

## 🧠 기본 개념
- HashSet: 중복을 허용하지 않는 집합 자료구조. insert() 시 이미 존재하는 값이면 false를 반환.
- Vec::retain(): 조건을 만족하는 요소만 벡터에 남기고 나머지를 제거.
- 이 둘을 조합하면, 처음 등장한 요소만 유지하고 이후 중복은 제거할 수 있습니다.

## 🔍 예제 코드
```rust
use std::collections::HashSet;

fn main() {
    let mut v = vec![3, 1, 2, 3, 2, 1];
    let mut seen = HashSet::new();
    v.retain(|x| seen.insert(*x));
    println!("{:?}", v); // 출력: [3, 1, 2]
}
```
- seen.insert(*x)는 처음 등장한 값이면 true를 반환하고, 중복이면 false.
- retain()은 true일 때만 해당 요소를 유지하므로, 중복은 제거됨.

## 🧩 사용자 정의 타입에서도 사용 가능
```rust
use std::collections::HashSet;

#[derive(Debug, Hash, PartialEq, Eq)]
struct Person {
    name: String,
    age: u32,
}
```
```rust
fn main() {
    let mut people = vec![
        Person { name: "Alice".into(), age: 30 },
        Person { name: "Bob".into(), age: 25 },
        Person { name: "Alice".into(), age: 30 },
    ];

    let mut seen = HashSet::new();
    people.retain(|p| seen.insert(p));
    println!("{:?}", people); // 출력: Alice, Bob
}
```
- HashSet을 사용하려면 Hash, Eq, PartialEq 트레잇을 구현해야 합니다.

### ✅ 장점
- 순서 유지: 원래 벡터의 순서가 그대로 유지됩니다.
- 빠름: HashSet의 insert()는 평균적으로 O(1) 시간 복잡도.
- 간단함: 코드가 직관적이고 짧음.

### ⚠️ 주의할 점
- HashSet은 요소의 해시값을 기준으로 중복을 판단하므로, Hash와 Eq 트레잇이 필요합니다.
- retain()은 벡터를 직접 수정하므로, 원본을 유지하려면 복사본을 사용해야 합니다.

### 🧪 변형 예제: 중복 제거 기준을 커스터마이징
```rust
use std::collections::HashSet;

fn main() {
    let mut words = vec!["apple", "apricot", "banana", "blueberry"];
    let mut seen = HashSet::new();
    words.retain(|word| seen.insert(word.chars().next().unwrap()));
    println!("{:?}", words); // 출력: ["apple", "banana"]
}
```
- 첫 글자를 기준으로 중복 제거.

## 📋 요약
| 특징             | 설명                                      |
|------------------|-------------------------------------------|
| 중복 제거 방식   | 전체 중복 제거                            |
| 순서 유지 여부   | ✅ 유지됨                                 |
| 성능             | 빠름 (HashSet 기반)                       |
| 사용 조건        | 요소가 Hash + Eq 트레잇을 구현해야 함     |
| 대표 예제        | `retain(|x| seen.insert(*x))`             |

---

# v.into_iter().collect::<HashSet<_>>().into_iter().collect() 설명

이 문장은 Rust에서 벡터의 중복 요소를 제거하기 위해 자주 사용되는 패턴입니다.  
아래에 단계별로 분해해서 설명:

## 🧾 전체 코드
```rust
let v: Vec<_> = v.into_iter().collect::<HashSet<_>>().into_iter().collect();
```

## 🧩 단계별 설명
### 1. v.into_iter()
- v는 Vec<T>입니다.
- into_iter()는 벡터를 소유권을 넘기는 이터레이터로 바꿉니다.
- 결과: IntoIter<T> 타입의 이터레이터가 생성됩니다.

### 2. .collect::<HashSet<_>>()
- 이터레이터를 collect()하여 HashSet으로 변환합니다.
- HashSet은 중복을 허용하지 않기 때문에, 이 과정에서 중복 요소가 제거됩니다.
- ::<HashSet<_>>는 타입 힌트입니다. Rust는 어떤 컬렉션으로 수집할지 명확히 알 수 없기 때문에 명시해줘야 합니다.

### 3. .into_iter()
- 이제 HashSet<T>를 다시 이터레이터로 바꿉니다.
- into_iter()는 HashSet의 요소들을 하나씩 꺼낼 수 있는 이터레이터를 반환합니다.

### 4. .collect()
- 이터레이터를 다시 Vec<T>로 수집합니다.
- 최종적으로 중복이 제거된 벡터가 생성됩니다.

### 📦 전체 흐름 요약
```
Vec<T>
  ↓ into_iter()
Iterator<Item = T>
  ↓ collect::<HashSet<_>>()
HashSet<T> (중복 제거됨)
  ↓ into_iter()
Iterator<Item = T>
  ↓ collect()
Vec<T> (중복 제거된 새 벡터)
```

### 🧪 예제
```rust
use std::collections::HashSet;

fn main() {
    let v = vec![1, 2, 2, 3, 1, 4];
    let v: Vec<_> = v.into_iter().collect::<HashSet<_>>().into_iter().collect();
    println!("{:?}", v); // 출력 예: [1, 2, 3, 4] (순서는 보장되지 않음)
}
```
- 주의: HashSet은 순서를 보장하지 않기 때문에, 결과 벡터의 순서는 원래와 다를 수 있습니다.
